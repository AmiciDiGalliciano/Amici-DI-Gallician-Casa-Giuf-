## 2026-06-12T18:37:33Z
You are a Translation Worker. Task: Translate the file `/Users/iMac21/Downloads/amici-di-galliciano-casa-giufa-v23-github-pages/el/index.html` from Italian to Modern Greek. 1. Read the file. 2. Translate all visible text and SEO texts (title, meta, etc.) into Modern Greek. 3. Keep all HTML tags, structure, and CSS classes intact. 4. Update asset paths: prepend `../` to any `assets/`, `downloads/`, and `manifest.json` links (e.g., `href="assets/css/style.css"` -> `href="../assets/css/style.css"`). Keep internal links to other HTML pages exactly as they are (e.g. `href="camere.html"`). 5. Overwrite the file `el/index.html` with the fully translated HTML using the `write_to_file` tool (set Overwrite=true). Do NOT omit any HTML or truncate the file. 6. When done, send a message back.
